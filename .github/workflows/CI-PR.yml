name: CI - PR
on:
  pull_request
jobs:
  Build:
    uses: nareshgdevops/roboshop-github-actions/.github/workflows/CI-PR.yml@main
    with:
      appType: maven
      appName: shipping
    secrets:
      VAULT_TOKEN: ${{ secrets.VAULT_TOKEN }}